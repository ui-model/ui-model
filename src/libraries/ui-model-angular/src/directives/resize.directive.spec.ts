/* tslint:disable:no-unused-variable */
import { ResizeDirective } from './resize.directive';

describe('ResizeDirective', () => {
  it('should create an instance', () => {
    let directive = new ResizeDirective();
    expect(directive).toBeTruthy();
  });
});
