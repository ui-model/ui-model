/* tslint:disable:no-unused-variable */
import {PagerDirective} from './pager.directive';

describe('PagerDirective', () => {
  it('should create an instance', () => {
    let directive = new PagerDirective();
    expect(directive).toBeTruthy();
  });
});
