/* tslint:disable:no-unused-variable */
import {ResourceUrlPipe} from './resource-url.pipe';

describe('ResourceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
