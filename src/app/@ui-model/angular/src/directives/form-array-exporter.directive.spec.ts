import { FormArrayExporterDirective } from './form-array-exporter.directive';

describe('FormArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new FormArrayExporterDirective();
    expect(directive).toBeTruthy();
  });
});
