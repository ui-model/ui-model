import { FormGroupExporterDirective } from './form-group-exporter.directive';

describe('FormGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupExporterDirective();
    expect(directive).toBeTruthy();
  });
});
