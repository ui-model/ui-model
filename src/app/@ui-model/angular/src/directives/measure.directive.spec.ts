/* tslint:disable:no-unused-variable */
import {MeasureDirective} from './measure.directive';

describe('MeasureDirective', () => {
  it('should create an instance', () => {
    const directive = new MeasureDirective();
    expect(directive).toBeTruthy();
  });
});
