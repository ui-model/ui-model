import { FormArrayDirective } from './form-array.directive';

describe('FormArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new FormArrayDirective();
    expect(directive).toBeTruthy();
  });
});
