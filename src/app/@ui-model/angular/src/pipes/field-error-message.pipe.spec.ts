import { FieldErrorMessagePipe } from './field-error-message.pipe';

describe('FieldErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
