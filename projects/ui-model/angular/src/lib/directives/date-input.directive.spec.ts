import { DateInputDirective } from './date-input.directive';

describe('DateInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DateInputDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
