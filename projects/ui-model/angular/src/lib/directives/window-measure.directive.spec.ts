import { WindowMeasureDirective } from './window-measure.directive';

describe('WindowMeasureDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowMeasureDirective();
    expect(directive).toBeTruthy();
  });
});
