import { FieldMetadataDirective } from './field-metadata.directive';

describe('FormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldMetadataDirective();
    expect(directive).toBeTruthy();
  });
});
